### Steps to build headless hidden console

- Run build.sh bash script
- It generate dist directory at root directory
- Use vbs to exe program:
    - open (root-dir)/script/launch.vbs
    - choose icon (root-dir)/assets/icon.ico
    - set exe format to 64 bit invisible
    - Compile to exe